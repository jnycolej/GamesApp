{
  "name": "games-app",
  "private": true,
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "heroku-prebuild": "npm install --prefix backend",
    "heroku-postbuild": "npm install --prefix frontend-vite && npm run build --prefix frontend-vite"
  }
}
